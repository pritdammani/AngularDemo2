import { ZoomdDirective } from './zoomd.directive';

describe('ZoomdDirective', () => {
  it('should create an instance', () => {
    const directive = new ZoomdDirective();
    expect(directive).toBeTruthy();
  });
});
